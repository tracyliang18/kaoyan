今天计划

1.英语单词 1.5h 
2.概率论大数定理 
3.线代 正交矩阵 1h
4.高数一套题
5.微分方程小练 1h
6.组原！！！！

1、单词
完成
2、大数定理完成。
大数定理关键是要理解不等式的含义。
当一个要期望为e，方差为d的分布执行很多次，则累加取平均趋向于一个常数，就是分布的均值，这时候方差为零。
而累加分布则趋向于一个期望为nu，方差为nd的正态分布。
回忆下常见分布的均值和方差
离散
二元分布
泊松分布
几何分布
超几何分布

连续
指数分布
正态分布
均匀分布
随机变量函数的数字特征注意不用另外求分布率，直接用原来的即可。



3.线代 没看

4.高数一套题 第七套
1.多元函数极值今天做了一道蛮爽的题目，多元函数在一定区域的最大最小值问题，关键是找到区域内的可疑极值点和边界。
在找边界的最大最小值时，有三种方法，答案（）
1.参数法，根据边界条件参数化x、y，转化为一元极值问题
2.lagelangri乘数法
3.我开始没反映过来，用高中的方法做，边界条件是一个椭圆，函数通过一个很巧妙的加减法化成很简单的形势（2x方+y防=1）希望可以记得，当时想到这个蛮爽的！
2.多元微分的时候，只用写F1，大大简化抄写量和减少出错！
3.平面几何间的距离问题不要忘记了，不过关键都是长度映射sin 或者 cos，心态好就没问题。
4.最后一条神题，二元函数却一直用一元的方法证，感觉很奇妙。关键还是用到了不定积分，还是那一句，证不等式关键还是变形，最后才是缩放！
题目是知道了二阶偏导的范围 f``（xy） 《 M， 关键是变形得出二阶偏导在缩放，
得出这个东西我第一时间想到的是二阶泰勒公式。。。做不出来。。。
积分证明看来分布积分很重要。。。。


组原：
   稍微看了控制器微指令一块，感觉还是一如既往的坑跌。。。硬布线较快，微指令较灵活。。微指令实际就是cpu内部又搞了一套粒度更少的指令集合，且指令可以说完全面向硬件了！注意在控制器是对不同类的指令进行分别的解释，也就是说同类指令有很多相似的地方，这也是微指令能应用的原因。注意指令内切换和指令间切换
。

微分方程
看了应该有两小时的微分方程，对其了解程度增加了一点，但还是只会做简单题
一阶：
可分离变量
齐次（通过变量带换转化为可分离变量） z = y/x  dzx + dxz = dy
伯努利方程 z=y^(1-n)
全微分方程（可以练习格林定理、积分和路径无关）

多阶可降阶
1. y(n) = f(x)
2. 没有y型 p = dy/dx d2y/dx2 = dp/dx
3. 没有x行 p = dy/dx d2y/dx2 = dp/dy * dy / dx = dp/dy * p

二阶常系数
齐次
1.ex + ex
2. ex + xex
3. eax（Acosbx + Bsinbx）

非齐次
1.eaxPn
    1.a不为特征解， eaxpn
    2.a为特征解，不为重解， xeaxpn
    3.a为重解， x2eaxpn

2.eaxcosbx
    1.a+bi 不解 eax（pnxcosbx + pnxsinbx）
    2.a + bi  为解 xeax（pnxcosbx + pnxsinbx）

当非齐次项不为以上两种时候，多注意非齐次方程的叠加性质。

明天计划：
1.看完《每个程序员都应该了解的内存知识》
2.写两篇英语作文！！
3.买1000题
4.线代2h
5.高数一套 八
6.单词 1h
7.数据结构---树
8.概率论统计部分

