今日简介：
1。线性代数巩固了矩阵
2. 高数一套
3. 政治
4.英语单词 
5. 计算机网络 看了路由器一点点


1.由于已经断电了，，（以后要早写总结了）
只能简单说以下

1.矩阵的基本运算 T -1 幂 *（代数yuzishi）
    T -1 幂 *都可以内外替换？
    (A)-1 ^ m = A ^m -1
    （A*）* =  |A|^(n-2)A

    矩阵乘法要区别常数乘法
    矩阵求逆有多种方法
        1.初等变换（行比较熟悉），变换的过程争取少写中间过程（节省草稿）
        2.A*法，对于小型矩阵还是可以用的，而且可以用来检查
        3.定义法，一般是填空题吧

    对称矩阵
        QT M Q 可以用来证明对称性，尤其是M为对角

    对角乘法非常方便

    对角的逆，逆对角的逆，

    区分行列式的分块和矩阵的分块

    区分行列式乘法和矩阵乘法

政治
    深坑。。真不知到怎么复习。。

英语单词今天背的效率不错

计算机网络
    看了路由器结构，准备到IP多播了

高数一套
    今天高数做的不是很顺手，积分有些手生了

    1.幂要学会取对数
    2.lnx x分布积分
    3.1/sinx 之类的积分公式记不住
    4.求曲面积分投影面要联立方程得到，不要只画图(有时很难想象到的）
    5.系数问题。。
    6.有一个求散度求错了。。。。

最近都没有很明确的计划了，而且计划执行的不够好
1.单词1h
2.买660，数学真题，政治真题
3.概率论一套
4.线代一套
5.高数一套
6.政治2h
7，network结束网络层，开始传输层
