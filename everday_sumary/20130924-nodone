今天计划：
1.线性代数一套
2.概率论数理统计 1h
3.单词 1.5h
4.高数一套
5.数据结构 图
6.操作系统1h
7.组原 1h

1.线代，完成
    1.注意A*行列式的值
    2.箭型行列式的计算，将第一列或第一行化为只有一个非零
    3.矩阵乘法是注意是否有某一个矩阵是初等矩阵，若是则大大减少计算量。
    4.方阵的幂联想相似
    5.ATA=O，则A=0（向量自身点乘）
    6.同方程组解的系数矩阵秩相等。
    7.注意区分矩阵相似、合同、等价的关系
    8.对正定不熟系
    9.注意题目中提示的实对称矩阵，实对称矩阵不同特征值的特征向量必正交，必相似于对角矩阵。

2.概率统计 
    半小时吧，较之前较好地理解抽样统计量分布。
    统计量难理解的地方是，它既可以是看作已知的（抽取后），也可以看作是未知的（抽前），统计量是多次随机变量的函数，因此它也是随机变量。
    典型的统计量有样本均值，样本方差。其分布也比较特殊，应该记住。
    书中首先研究了总体为正态分布的情况，这里要说明的是，总体是正态分布是，很多统计量的分布是已知的，然而当总体分布不是正态是，只能有多次抽样（n足够大是），利用中心极限定理构造正态分布。
    1.单正态分布
        样本average  E u D D/n
        方差 S2      E D
    2.双正态独立分布
        样本average E u1+u2 D D1/n1+D2/n2
    3.非正态
        n较大是才有1的近似于1的结论

    几个统计量常用的分布
        1.X2分布 联系到无穷积分
            稍微扩展以下，无穷积分F函数，F（1/2）=跟号pi e^(-x2) 在R上积分也为跟号pi
            F（1） = 1 F（n+1）=nF（n）=n！（n 》=1）

            在总体遵循标正态时，X1^2 + X2^2 + ... Xn^2 遵循 X2（n）分布
                一般正态时候，可以把一般正态变为标准正态有一样的结果
                （n-1)*S2/D ~ X2(n-1)

            X2（n）均值 n， 方差 2n
        2.t分布
            X/跟号（Y/n） 其中X标正态， Y ～ X2（n）
            t > 30, 近似与标正态

        3.F分布， 两个分布分别 是 n1 n2的X2分布
            X/n1 / Y/n2 ~ F(n1,n2)

        D（X） = E（x2） - E(x)2 非常有用

3.单词 兼Chinglish学习 完成
   
4.高数 没做 

5.额外 高数强化了曲线积分和格林定理
    1.巩固了参数方程的曲线积分，别忘记平方了！！
    2.格林定理 ！！ Qx - Py， 注意曲线为正向，另外补曲线时注意补的是正向
    3.利用对偶性，这个不多说了

6.数据结构 图
    图 围绕 有向 无向的区别进行
    1.储存结构
        临接矩阵 有向，无向 ，注意稀疏图 E < Vlog(v)
    2. 邻接表
        每个定点储存一个链表，链表是由此出发的边
        逆邻接表——到这里的边
        因此出现了十字邻接表（有向） 邻接多重表（无向）
    3.连通
        有向：存在强连通
    4.最小生成树算法 prime and what？
        prime好理解，不断融合最小代价的边到顶点集合中
    5.拓扑排序
        拓扑排序解决事情的序列问题，
    6.关键路径
        以拓扑排序为基础，寻找一条最影响整个工程完成的路径。关键是完成两个数组信息的更新。
            1.事件（顶点）的最先开始时间 从拓扑定点开始逐渐递推 max（前面定点的最先完成 + 路径代价）
            2.事件（顶点）的最晚开始时间 从拓扑结束开始逐渐倒退 min（当前边结束点最晚完成 - 路径代价）

            定点为 最先开始和最晚开始相等的有可能在关键路径，这要再做检查

    7.最短路径
        dijiekesila算法 也是不断加入最小代价定点到定点集合直到所有顶点都加入。
        算法本身复杂度o（n2）
        若找到图中所有点两两之间的最短距离，则要o（n3）
        floye算法没了解。。

    8.遍历
    图一类东西还是看看topcoder的教程吧

7.操作系统 进程
    1.我觉得这块理解为主，死记硬背没有意义。
        信号量多找些练习连连，写写代码

8.组原
    没看

下面总结下这两天看Chinglish的感受
1.确实周围都充斥这中式英语，不过这也是在所难免的。一是中国语言习惯和native英语就有差别，二来中国官方讲话影响深远，清嗓子之类的话太多，三是教育的原因，你看看所谓的好词组，好模板，我想大多是cE，这里一是我们学习的资料有问题，教学有问题，二来是学生接触到的native英语机会实在太少，词汇量和因缺少想表达的用词有或者直接将中文句式毫无保留地直译成英语都是Ce产生的原因。

不过看这个用来准备考研英语作文，我觉得太高级了，毕竟阅卷老师可能本身就只会ce，二来自己确实也写了太多年ce了，如果不写ce觉得真的是词穷了。

不过我倒是有个中间的办法
    1.看看别人的chinglish，觉得还行的留做备用，呆词穷的时候用
    2.学习真正colorful，not-general用词的方法，加强特色词的训练。

下面摘抄一些unnecessary n and v
to accelerate "the pace of" economic reform
we should adopt "a series of" measures to ensure that
at that time the situation in northeast China was still one where the enmey was stronger than the people's force.
the key to solution lies in the curtailment of expenditure.
to ensure close cooperation

v
it is impossible for us to accomplish the transformation of the whole society overnight.
we should conduct a careful examination of ..

