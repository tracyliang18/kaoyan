今日简介
1.背单词
2.线性代数做了一套，向量组一块感觉比较弱，行列式计算也要多练习
3.计算机网络
4.概率论

1.概率论
    1.D（X） = E（X^2) - E(x)^2 可以说是必考。。。

2.线性代数
    二次型正定的congyao条件： 顺序字行列式 > 0
    2.反对角行列式
        1.不分快
            （-1）^(n)(n-1)
        2.分块
            （-1）^(m*n)
        3.用较少数量向量组可以表达较多数量向量组，则后者线性相关
        4.合同于单位矩阵充要：正定矩阵
        5.相似于单位矩阵：为单位矩阵
        6.等价于单位矩阵：为可逆矩阵
        7. M*N r（A）=n， 则ATA 为正定矩阵 XTATAX = （AX）T *（AX）对X！=0
        8.主要方程组行满秩则一定有解（不管是齐次还是非齐次
            行manzhi和列满秩需要关注，秩的不等式（小于行列往往有用）
            系数矩阵的秩少于等于扩展矩阵的秩
        9.注意齐次方程的解结构，关键是基向量及数量的确定
        10。lapulasi定理？？这个不懂
        11.若矩阵的秩为
        12.向量组表示，被表示的秩小于等于用于表示的
            由此推出等价必等秩
        13.注意向量组的包含关系
            1.最大无关组在向量组内，因此最大无关组等价于向量组
        14.向量组的多个最大无关组等价（并且只要达到了数量要求就ok）
        15.常用：无关向量组加一个向量变成相关，则新增向量可用原无关的表示
        16.若两向量组等秩，则且有其中一个表示关系，则可以证明等价关系
            等秩是等价的必要条件
        27.矩阵秩为1则可以找到 ATB的向量成绩形式。
        28.正交变换二次性注意特征值与特征向量的对应关系。
        29.正负惯性指数的应用，构造向量y，然后得出非零想零x
        30.特征向量个数的关系
            若特征值构成的矩阵（求行列式哪个）的秩为t
            则该特征值至少是n-t重的
3.计算机网络
    传输层
    
