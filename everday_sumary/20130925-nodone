今天计划
1.概率论 参数估计
2.单词 1h
3.ds 查找
4.组原 1h
5.os 处理机死锁
6.高数现代各一套

1.概率论参数估计
    这里的参数有几个含义，
    一是分布的数字特征，期望，方差等
    二是可能是一些具体影响分布的参数，例如正态分布的参数就是均值和方差，X2分布是自由度n，posong分布，指数分布，均匀分布等
    首先估计有两类估计，一是已知分布，二是未知分布。
    未知分布的我们可以通过样本构成函数估计期望和方差，可以利用一阶和二阶原点矩去估计，这就是点估计，如果形成一定范围的参数，则是区间估计
    如果知道分布的话用极大似然估计效果更佳，其原理是找到参数来让已发生的样本在估计的总体上发生的概率最大
    不知到大纲这一快是怎么弄的。。

2.单词
    完成，ce还在看，今天看的是

3.查找
    1.静态动态
    2.顺序 分块 二分 注意复杂度 前两个是o（n）级别的。 二分是log级别的
    3.Btree 明显减少IO操作，磁盘寻找柱面的时间。
        插入：注意分裂，中间点向上升
        删除：归结到删除底层节点的逻辑
            但删除节点后容易会式结构不再符合Btree的特征，这时候需要调整
                1.如果左右兄弟子树有多的，夫节点分一个最小的过来，右子树分一个最小的到夫节点
                2.左右子树都没有多余的节点了，则整个节点连同夫节点指向本节点的关键字一并合并到兄弟节点。此事可能夫节点的关键字过少，这时在对夫节点向上合并

    hash比较简单，注意处理冲突的方法就好。

4.组原 
    稍微复习了编码一块。。坑跌的组原
    1。进制转换 
        十进制转x进制 整数 除r取余数 注意倒着取 小数部分不断乘以r，从高往下取，高是小数点第一位
        2、8、16转换easy
    2.bcd注意压缩非压缩就好，非压缩一个字节前四位都是0.。。。
    3.校验码
        1.奇偶校验 
            检测一位错误
            奇校验 ： 包括校验位，使1的个数为奇数
            偶                               偶数

            所以检查的时候全部异或，奇检验为1，偶检验为0
        2.行列奇偶校验
            可能检测偶数位错误
            也有纠正功能
    4.CRC
        确定冗余位数
        编码移位
        除以生成多项式
        模二除法
        编码加余数等于编码，检测时编码除以生成多项式，有余数则错误

5.os 处理机和死锁，没看

6.线代一套，高数没做
    1.发现重要规律一条，当有说明矩阵A ！= 矩阵B时，通常会变形出（A-B），从而得出A——B为非零矩阵，非零矩阵可能于秩，方程解，相关。
    2.当两矩阵乘积等于零时，要记住用R（A） + （R） 《 n， 而若其中之一为非零矩阵的话，则两个都不可逆。
    3.注意方程有解一定不能忘了系数矩阵的秩要和zhengguang矩阵的秩相等，不然即使r（A） 《 n也不会有无穷组解。
    4.正交矩阵行（列）向量组为标准正交。
    5.注意线性方程组解的构成
        齐次： 1.唯一解 0
               2.多解 K1X1 + K2X2.。。
        非齐次：
                1.无解
                2.唯一解
                3.多解  齐次的同解+特征解

                于是
                    若x1 x2 x3。。。位非齐次方程的解
                        则
                            k1x1 + k2x2 + k3x3 + ... knxn 也为方程组的解，注意k1+k2+..KN=1
                    XI - XJ  为齐次的解
    6.AB = O 可以看作是AX=0的方程，其中X构成了B。
    7.对角矩阵好性质：对称，乘法，行列式，特征值。。

明天计划：
    1.尝试用纸写总结吧，在电脑上敲，很多东西总结不过来。而且输入法很不给力
    2.单词1h，录入google 1h
    3.os 1h 处理机
    4.ds 排序
    5.co 运算器 kengxialai！！！
    6.高数一套
    7.现代一套
    8.概率论假设检验
