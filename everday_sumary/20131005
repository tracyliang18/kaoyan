今日简介
    1.打了场球，糟糕的发挥
    2.上午九点到11点半，下午2点到3点45，晚上七点到10点
    
    1.解决了文件系统，但感觉计算还是很容易错啊
    2.做了一套线性代数，线性代数的训练量明显比高数小
    3.做了一套高数

    1.文件系统
        对某些专业名词需要深入理解，由于名词翻译的原因导致不是很好理解。。

    2.线代
        二次正定型

    3.高数

今天打了2k 1vs1又这个点才高总结。。。
20131006
简介
1.计算机看完了数据链路层
2.计算机组成原理把总线巩固了。cpu也看了，不过没有做题。
3.线代做了一套
    线代巩固了相似关系和合同关系
4.英语真题一套

数据链路层
1.争用期2r相关的数据要明白，10M以太网为51.2us，也就是64比特时间， 最小有效比特 64bit等等。。
2.mac帧的格式。。最大可以多少数据，帧长最大多少要熟记
3.总线半双工网络利用率 1/（1+a） a = r/T = rC/L c为速度。L为帧长，当传输速率提高，则应降低争用期降低（相当与更频繁去检测信道吧）
4.交换机。。冲突域。。

2.总线
    1.关键在总线判决吧（链式、计数、独立）
    2.总线周期（在总线传输一次数据所用时间）
    3.PCI总线比较重要？其他总线也关注下？

    cpu：
        对各类指令的数据流要很清楚地说出来。
        微指令设计、硬布线还不知道要考到什么程度。

3.英语真题一套：
    英语，完形错了7个，阅读错了5个，排序全对。
    下午做的英语，由于昨晚没睡好。。所以很困。现在应该开始调整作息？
    用的时间非常长，今天看的文章比较吃力。很多基本上是用排除法。具体原因没有详细分析。

